import { IHttpLlmFunction } from "../structures/IHttpLlmFunction";
import { ILlmSchema } from "../structures/ILlmSchema";
export declare namespace LlmSchemaSeparator {
    interface IProps {
        parameters: ILlmSchema[];
        predicator: (schema: ILlmSchema) => boolean;
    }
    const parameters: (props: IProps) => IHttpLlmFunction.ISeparated;
    const schema: (predicator: (schema: ILlmSchema) => boolean) => (input: ILlmSchema) => [ILlmSchema | null, ILlmSchema | null];
}
