"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.StringUtil = void 0;
var NamingConvention_1 = require("./NamingConvention");
var StringUtil;
(function (StringUtil) {
    StringUtil.capitalize = function (str) {
        return str.length !== 0 ? str[0].toUpperCase() + str.slice(1).toLowerCase() : str;
    };
    StringUtil.pascal = function (path) {
        return StringUtil.splitWithNormalization(path)
            .filter(function (str) { return str[0] !== "{"; })
            .map(NamingConvention_1.NamingConvention.pascal)
            .join("");
    };
    StringUtil.splitWithNormalization = function (path) {
        return path
            .split("/")
            .map(function (str) { return StringUtil.normalize(str.trim()); })
            .filter(function (str) { return !!str.length; });
    };
    StringUtil.reJoinWithDecimalParameters = function (path) {
        return path
            .split("/")
            .filter(function (str) { return !!str.length; })
            .map(function (str) {
            return str[0] === "{" && str[str.length - 1] === "}"
                ? ":".concat(str.substring(1, str.length - 1))
                : str;
        })
            .join("/");
    };
    StringUtil.normalize = function (str) {
        str = str.split(".").join("_").split("-").join("_").trim();
        if (RESERVED.has(str))
            return "_".concat(str);
        else if (str.length !== 0 && "0" <= str[0] && str[0] <= "9")
            str = "_".concat(str);
        return str;
    };
    StringUtil.escapeDuplicate = function (keep) {
        return function (change) {
            return keep.includes(change) ? StringUtil.escapeDuplicate(keep)("_".concat(change)) : change;
        };
    };
})(StringUtil || (exports.StringUtil = StringUtil = {}));
var RESERVED = new Set([
    "break",
    "case",
    "catch",
    "class",
    "const",
    "continue",
    "debugger",
    "default",
    "delete",
    "do",
    "else",
    "enum",
    "export",
    "extends",
    "false",
    "finally",
    "for",
    "function",
    "if",
    "import",
    "in",
    "instanceof",
    "module",
    "new",
    "null",
    "package",
    "public",
    "private",
    "protected",
    "return",
    "super",
    "switch",
    "this",
    "throw",
    "true",
    "try",
    "typeof",
    "var",
    "void",
    "while",
    "with",
]);
