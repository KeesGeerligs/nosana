import { LowSync } from 'lowdb/lib';
import Logger from '../../logger/index.js';
import { DockerExtended } from '../../../../docker/index.js';
import { RequiredResource, Resource } from '../../../../types/resources.js';
import { NodeDb } from '../../db/index.js';
export declare function createResourceName(resource: RequiredResource): string;
/**
 * Creates Volume Manager Sub-module
 * @param db
 * @param docker
 * @returns
 */
export declare function createVolumeManager(db: LowSync<NodeDb>, docker: DockerExtended, logger: Logger): {
    createRemoteVolume: (resource: RequiredResource | Resource) => Promise<string>;
    getVolume: (resource: RequiredResource | Resource) => string | undefined;
    hasVolume: (resource: Resource) => Promise<boolean>;
    setVolume: (bucket: string, volume: string) => void;
    resyncResourcesDB: () => Promise<void>;
    pullMarketRequiredVolumes: (remoteResources: RequiredResource[]) => Promise<void>;
    pruneVolumes: () => Promise<void>;
};
