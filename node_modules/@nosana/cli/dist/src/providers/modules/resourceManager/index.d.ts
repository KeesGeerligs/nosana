import { LowSync } from 'lowdb/lib';
import { DockerExtended } from '../../../docker/index.js';
import { RequiredResource, Resource } from '../../../types/resources.js';
import Logger from '../logger/index.js';
import { NodeDb } from '../db/index.js';
export type ResourceManager = {
    resyncResourcesDB: () => Promise<void>;
    fetchMarketRequiredResources: (market: string) => Promise<void>;
    prune: () => Promise<void>;
    images: {
        setImage: (image: string) => void;
    };
    volumes: {
        getVolume: (resource: Resource) => string | undefined;
        hasVolume: (resource: Resource) => Promise<boolean>;
        setVolume: (bucket: string, volume: string) => void;
        createRemoteVolume: (resource: RequiredResource | Resource) => Promise<string>;
    };
};
export declare function createResourceManager(db: LowSync<NodeDb>, docker: DockerExtended, logger: Logger): ResourceManager;
