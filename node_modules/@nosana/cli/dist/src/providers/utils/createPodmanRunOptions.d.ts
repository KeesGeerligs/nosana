import { type RunContainerArgs } from '../DockerProvider.js';
/**
 * Takes image and args and return podman run options
 * @param image
 * @param args
 * @param gpu
 * @returns
 */
export declare function createPodmanRunOptions(image: string, args: RunContainerArgs, gpuOption: string): {
    env: {
        [key: string]: string;
    } | undefined;
    devices: {
        path: string;
    }[];
    restart_policy: "" | "unless-stopped" | "always" | "on-failure" | undefined;
    netns: {
        nsmode: "bridge" | "host" | "none";
    };
    Networks: {
        [key: string]: {};
    } | undefined;
    create_working_dir: boolean;
    cgroups_mode: string;
    work_dir: string | undefined;
    entrypoint?: string[] | undefined;
    image: string;
    name: string | undefined;
    command: string[] | undefined;
    volumes: {
        dest: string;
        name: string;
        Options: string[];
    }[] | undefined;
};
