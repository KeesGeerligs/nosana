import { Container, ContainerInspectInfo } from 'dockerode';
export declare function getCotnainerInfo(container: Container, timeout?: number, controller?: AbortController): Promise<ContainerInspectInfo>;
