import { textOutputEventHandlers } from './TextOutputEventHandlers.js';
export class TextOutputFormatter {
    finalize() { }
    output(event, param) {
        textOutputEventHandlers[event](param);
    }
}
