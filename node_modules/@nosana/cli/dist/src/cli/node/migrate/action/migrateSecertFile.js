import fs from 'fs';
import path from 'path';
export function migrateSecertFile(walletPath, suspectedPublicKey) {
    const compromisedPath = path.join(path.dirname(walletPath), `${path.basename(walletPath)}.compromised.${suspectedPublicKey}`);
    fs.renameSync(walletPath, compromisedPath);
}
