import { Command } from 'commander';
import { Client } from '@nosana/sdk';
export declare function download(ipfshash: string, path: string | boolean | undefined, options: {
    [key: string]: any;
}, cmd: Command | undefined, nosana?: Client): Promise<void>;
