import JobManager from '../../../services/JobManager/index.js';
export const jobServe = ({ config, port }) => {
    const jobManager = new JobManager(config);
    if (Number(port)) {
        jobManager.listen(parseInt(port));
    }
    else {
        console.log('Port must be a number.');
    }
};
