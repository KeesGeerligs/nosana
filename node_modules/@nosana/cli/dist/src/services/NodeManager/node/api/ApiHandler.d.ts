import { Client as SDK } from '@nosana/sdk';
import { Provider } from '../../provider/Provider.js';
import { NodeRepository } from '../../repository/NodeRepository.js';
export declare class ApiHandler {
    private sdk;
    private repository;
    private provider;
    private port;
    private api;
    private address;
    private flowHandler;
    private server;
    private wss;
    private eventEmitter;
    private tunnelCheckInterval;
    constructor(sdk: SDK, repository: NodeRepository, provider: Provider, port: number);
    start(): Promise<string>;
    private restartTunnelAndProxy;
    private stopTunnelAndProxy;
    private startWebSocketServer;
    private startTunnelCheck;
    private registerRoutes;
    private listen;
    private stopTunnelCheck;
    stop(): Promise<void>;
}
