import { Job, Client as SDK } from '@nosana/sdk';
import { NodeRepository } from '../../repository/NodeRepository.js';
import { JobDefinition, FlowState } from '../../provider/types.js';
export declare class JobExternalUtil {
    private sdk;
    private repository;
    constructor(sdk: SDK, repository: NodeRepository);
    resolveJobDefinition(id: string, job: Job): Promise<JobDefinition>;
    resolveResult(id: string): Promise<FlowState>;
    validate(id: string, jobDefinition: JobDefinition): Promise<boolean>;
}
