import { Flow, JobDefinition } from '../../provider/types.js';
import { Provider } from '../../provider/Provider.js';
import { NodeRepository } from '../../repository/NodeRepository.js';
export declare class FlowHandler {
    private provider;
    private repository;
    constructor(provider: Provider, repository: NodeRepository);
    init(id: string): void;
    start(id: string, jobDefinition: JobDefinition): Flow;
    resume(id: string): Flow;
    run(id: string): Promise<Flow>;
    stopCurrentFlow(): Promise<void>;
    stop(id: string): Promise<void>;
    private createInitFlow;
    private createNewFlow;
    private createOpNewState;
    exposed(id: string): boolean;
    private(id: string): boolean;
    operationExposed(id: string): string;
    clearOldFlows(): Promise<void>;
    generateRandomId(length: number): string;
}
