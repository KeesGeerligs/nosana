import { Provider } from '../../provider/Provider.js';
import { NodeRepository } from '../../repository/NodeRepository.js';
export declare class BenchmarkHandler {
    private provider;
    private repository;
    private flowHandler;
    constructor(provider: Provider, repository: NodeRepository);
    check(isInMarket: boolean): Promise<boolean>;
    private processSuccess;
    private parseLogsIntoJSON;
    private processFailure;
    private processSystemInfoBenchmark;
    private processNetworkInfoBenchmark;
    private processGPUInfoBenchmark;
}
