import { Ora } from 'ora';
import { LogObserver, NodeLogEntry } from '../NodeLog.js';
export declare const consoleLogging: () => ConsoleLogger;
export declare class ConsoleLogger implements LogObserver {
    private pending;
    private expecting;
    private progressBar;
    private layerIds;
    private multiProgressBar;
    private kill;
    private benchmarking;
    private running;
    spinner: Ora;
    constructor();
    addObserver(): void;
    update(log: NodeLogEntry, isNode?: boolean): void;
}
