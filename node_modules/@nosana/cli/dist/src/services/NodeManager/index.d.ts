export default class NodeManager {
    private node;
    private apiHandler;
    private exiting;
    inJobLoop: boolean;
    constructor(options: {
        [key: string]: any;
    });
    init(): Promise<void>;
    start(market?: string): Promise<void>;
    stop(): Promise<void>;
    restart(market?: string): Promise<void>;
    delay(sec: number): Promise<void>;
    error(): Promise<void>;
    /**
     * Set up handling for process exit signals
     */
    private handleProcessExit;
}
