import { JobState, JobStateData } from './types.js';
export declare const subscribe: (callback: (entry: {
    state: JobState;
    data: JobStateData[JobState];
    timestamp: Date;
}) => void) => void;
