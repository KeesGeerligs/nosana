import { NODE_STATE_NAME } from './types.js';
import { StateManager } from '../StateManager.js';
export class NodeStateManager extends StateManager {
    constructor() {
        super(NODE_STATE_NAME.NONE, {}, 'nodeStateChange', [
            'node',
            'market',
            'job',
        ]);
    }
}
