import { JobObject, JobPostingOptions } from './listener/types/index.js';
import { JobDefinition } from '../../providers/Provider.js';
import { JobManagerState } from './state/index.js';
export default class JobManager {
    protected state: JobManagerState;
    private nosana;
    private workers;
    constructor(configPath: string);
    status(id: string, onEvent: (msg: {}) => void, onClose: () => void): Promise<void>;
    post(market: string, job: JobDefinition, options: JobPostingOptions): Promise<JobObject>;
    get(id: string): JobObject | undefined;
    list(): string[];
    stop(id: string, waitForRunning?: boolean): Promise<string[]>;
    listen(port: number): void;
}
