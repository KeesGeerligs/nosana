import { Response, NextFunction } from 'express';
import { JobRequest } from '../types';
export declare function stopJob(req: JobRequest<{
    id: string;
}, {}>, res: Response, next: NextFunction): Promise<void>;
