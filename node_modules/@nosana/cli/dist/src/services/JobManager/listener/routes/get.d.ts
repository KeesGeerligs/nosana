import { NextFunction } from 'express';
import { JobObject, JobRequest, JobResponse } from '../types';
export declare function getJob(req: JobRequest<{
    id: string;
}>, res: JobResponse<{
    job: JobObject | undefined;
}>, next: NextFunction): void;
