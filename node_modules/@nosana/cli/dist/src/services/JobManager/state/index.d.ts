import { JobObject } from '../listener/types/index.js';
import { ReactiveEvent } from './helper/reactiveState.js';
export declare class JobManagerState {
    private db;
    private state;
    constructor(config: string);
    delete(key: string): void;
    get(key: string): JobObject | undefined;
    list(): JobObject[];
    has(key: string): boolean;
    set(key: string, stateOrCallback: JobObject | ((current: JobObject | undefined) => JobObject)): void;
    subscribe(key: string, callback: (event: ReactiveEvent, value: JobObject) => void): void;
    unsubscribe(key: string): void;
}
