import { postJob } from './postJob.js';
export function asyncPostJob(market, job) {
    return new Promise(async (resolve, reject) => {
        try {
            const result = await postJob(market, job);
            resolve(result);
        }
        catch (e) {
            reject(e);
        }
    });
}
