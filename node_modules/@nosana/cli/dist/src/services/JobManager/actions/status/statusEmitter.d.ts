/// <reference types="node" />
import EventEmitter from 'events';
import { JobResult } from '../../listener/types';
export declare class StatusEmitter extends EventEmitter {
    close(): void;
    emitCreate(job_id: string, job: JobResult): void;
    emitStatus(job_id: string, node: string, status: string | number): void;
    emitProgress(job_id: string, step: string): void;
}
