import createClient from 'openapi-fetch';
import { paths } from './schema.js';
type CreateClient = ReturnType<typeof createClient<paths>>;
export declare const clientSelector: () => CreateClient;
export {};
